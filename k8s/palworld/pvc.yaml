apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: palworld-server
  name: palworld-server-datadir
  namespace: palworld
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 75Gi